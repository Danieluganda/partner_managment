<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Partner Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>⚠️ Server Error</h1>
        <p class="subtitle">Something went wrong on our end.</p>
        
        <div class="alert">
            <strong>🔧 We're working on it:</strong>
            Please try again in a few moments or contact support if the problem persists.
        </div>

        <% if (typeof error !== 'undefined') { %>
        <div class="files-section">
            <h3>Error Details</h3>
            <ul class="file-list">
                <li>Error: <%= error.message %></li>
                <% if (error.stack) { %>
                <li>Stack: <%= error.stack.split('\n')[0] %></li>
                <% } %>
            </ul>
        </div>
        <% } %>

        <div class="btn-group">
            <a href="/" class="btn btn-primary">
                <div class="icon">🏠</div>
                <div class="btn-content">
                    <div class="btn-title">Go Home</div>
                    <div class="btn-desc">Return to the main dashboard page</div>
                </div>
            </a>
        </div>
    </div>
</body>
</html>